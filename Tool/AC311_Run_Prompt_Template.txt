Use [$ac311-tutor](C:/Users/ASUS/.codex/skills/ac311-tutor/SKILL.md), [$yuehua](C:/Users/ASUS/.codex/skills/yuehua/SKILL.md), and [$frontend-skill](C:/Users/ASUS/.codex/skills/frontend-skill/SKILL.md).

Work in:
D:\Fogust\Workspace\College\AC311\Cheat Sheet

Quick edit before each run:

- `TARGET_CHAPTER_NUMBER = 07`
- `TARGET_CHAPTER_LABEL = Chapter 7`
- `OUTPUT_PREFIX = chapter-07`

Run this as an on-demand workflow for `TARGET_CHAPTER_LABEL`.

Run in `strict workflow mode`.

Primary objective:
Teach `TARGET_CHAPTER_LABEL` from the beginning in a very intensive and highly detailed way, segment by segment, until the whole chapter is complete. For every segment, immediately follow this loop:

1. Teach the segment in Thai with English accounting terms, strong Intermediate Accounting depth, standard names, recognition and measurement logic, presentation logic, exam traps, and journal-entry implications where relevant.
2. Give a mini practice for that exact segment right away.
3. Provide the full solution and explanation right away.
4. Save the full raw output of that segment as `segment-XX.md` in the working directory.
5. Ask Yuehua to reorganize that segment into polished, readable, exam-focused notes and append the refined content into `OUTPUT_PREFIX-master-notes.md` without deleting prior refined segments.
6. Continue to the next segment and repeat the loop until `TARGET_CHAPTER_LABEL` is fully covered.

Strict execution requirements:

- Work on exactly one segment at a time.
- Do not teach multiple new segments in a single pass.
- Do not draft `segment-02.md` before `segment-01.md` has fully completed the loop.
- Do not batch all segments first and organize them later.
- For each segment, the order is mandatory:
  1. teach
  2. mini practice
  3. solution
  4. save `segment-XX.md`
  5. Yuehua reorganizes
  6. append into `OUTPUT_PREFIX-master-notes.md`
  7. only then move to the next segment
- Do not create the mock exam, frontend brief, or HTML until all segments have completed the full loop.
- If the workflow is interrupted, resume from the next unfinished step of the current segment, not from a future segment.

After all segments are complete:

1. Create a very difficult mixed-concept mock exam for `TARGET_CHAPTER_LABEL` and save it as `OUTPUT_PREFIX-mock-exam.md`.
2. Ask Yuehua to do a final full consolidation pass over all segment files, the cumulative notes, and the mock exam, then save the final organized note set as `OUTPUT_PREFIX-master-notes.md`.
3. Create `OUTPUT_PREFIX-frontend-brief.md` for the HTML build.
4. Build `OUTPUT_PREFIX-notes.html`.

Important execution rules:

- This is an on-demand workflow, not a scheduled automation.
- Start only when explicitly invoked by the user.
- Use `TARGET_CHAPTER_NUMBER` to identify the chapter resources and teaching scope.
- If previous segment files already exist, resume from the next unfinished segment instead of restarting, unless the user explicitly asks to restart from segment 1.
- Keep the teaching rigorous and exam-oriented, but still readable and humane.
- Do not ask the user whether they want mini practice; include it automatically as part of this workflow.
- Use stable file names and keep accumulating progress in the working directory.

Final HTML direction:

- minimal exam-focused lecture note
- warm but not sweet
- exam-ready but not rigid
- cream and light brown base
- gray-blue secondary information layer
- strong hierarchy in headings and summary boxes
- journal entry blocks more prominent but still restrained
- subtle paper texture
- use `TH Sarabun New` as the primary font
- soft typography that does not feel harsh or sharp

Font requirement:

- Use `TH Sarabun New` across the page as the default typeface for headings and body text.
- Do not substitute a different primary font unless the user explicitly changes the requirement.
